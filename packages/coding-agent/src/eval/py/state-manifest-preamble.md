The conversation was just compacted, but the persistent `eval` Python kernel was NOT reset — every variable, import, and function you defined is still live in that kernel and remains directly usable from any `eval` cell. The compaction only rewrote this transcript, so re-read the manifest below before recomputing anything: reference these bindings directly instead of rebuilding them.

